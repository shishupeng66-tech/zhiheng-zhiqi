# Phase 3-A 测试素材

`test_voice.mp3`（25KB） — 来自现有 voice-service 生成的合法 mp3 试听文件，**仅用于 Phase 3-A 端到端冒烟测试**。
（用户期望是 wav，但当前 voice-service 预设音色有 `resource ID is mismatched with speaker related resource` 问题，
不在 Phase 3-A 范围，故以 mp3 替代。mp3 在豆包 voice_clone 接受的 6 种格式内，等价有效。）
